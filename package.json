{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "pnpm -r --if-present run build",
    "typecheck": "pnpm -r --if-present run typecheck",
    "dev:backend": "pnpm --filter backend run dev:build",
    "dev:frontend": "pnpm --filter frontend run dev",
    "seed": "pnpm --filter backend run seed",
    "db:push": "pnpm --filter backend run db:push"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  }
}
